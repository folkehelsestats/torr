% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/narko-pop.R
\name{create_narko_pop_vec}
\alias{create_narko_pop_vec}
\title{Apply \code{create_narko_pop()} Over Multiple Specifications}
\usage{
create_narko_pop_vec(D, vars_list, vals, ...)
}
\arguments{
\item{D}{A data.frame or data.table to be augmented. The object is passed to and
returned from each call to \code{\link[=create_narko_pop]{create_narko_pop()}}.}

\item{vars_list}{A list where each element is a character vector of variable names to pass
as \code{vars} to \code{\link[=create_narko_pop]{create_narko_pop()}}. For example:
\code{list(c("ans2_a","ans3_1"), c("ans2_b","ans3_2"))}.}

\item{vals}{A character vector of the same length as \code{vars_list}. Each element is
passed as \code{val} to \code{\link[=create_narko_pop]{create_narko_pop()}} for the corresponding
\code{vars_list} entry.}

\item{...}{Additional arguments passed through to \code{\link[=create_narko_pop]{create_narko_pop()}}, if
supported by that function (e.g., \code{types}, \code{ans}, etc.).}
}
\value{
Returns the updated object \code{D} after all calls to
\code{\link[=create_narko_pop]{create_narko_pop()}} have been applied.
}
\description{
A convenience wrapper that applies \code{\link[=create_narko_pop]{create_narko_pop()}} repeatedly to
the same dataset for a set of \verb{(vars, val)} specifications. This helps avoid
writing multiple sequential calls and keeps analysis code concise and
consistent.
}
\details{
Internally, the function loops once over \code{seq_along(vals)}, updating \code{D}
in-place for each \verb{(vars, val)} pair by calling \code{\link[=create_narko_pop]{create_narko_pop()}}.
The order of \code{vars_list} / \code{vals} determines the order of application.

Basic validation is performed to ensure:
\itemize{
\item \code{vars_list} is a list.
\item \code{vals} is a character vector with the same length as \code{vars_list}.
\item Each element of \code{vars_list} is a non-empty character vector.
}
}
\examples{
\dontrun{
# Example specification (mirrors repeated calls):
vars_list <- list(
  c("ans2_a", "ans3_1"),
  c("ans2_b", "ans3_2"),
  c("ans2_c", "ans3_3"),
  c("ans2_e", "ans3_5"),
  c("ans2_f", "ans3_6"),
  c("ans2_g", "ans3_7"),
  c("ans2_h", "ans3_8")
)
vals <- c("kokain", "mdma", "amfetaminer", "heroin", "ghb", "lsd", "annet")

DD2 <- torr::create_narko_pop_vec(DD, vars_list, vals)
}

}
\seealso{
\code{\link[=create_narko_pop]{create_narko_pop()}}
}
